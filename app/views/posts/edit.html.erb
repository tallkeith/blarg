<%=  form_tag(edit_post_path(post), method: "patch") do %>
    <div class="post-field">
        <%= label_tag("Title: ") %> <br/>
        <%= text_field_tag("title", post.title) %>
    </div>

    <div class="post-field">
        <%= label_tag("Content: ") %> <br/>
        <%= text_area_tag("content", post.content) %>
    </div>

    <%= button_tag("Update Post!") %>
<% end %>