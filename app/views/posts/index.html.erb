<%= link_to("Add a New Post!", posts_new_path) %>

<h1>ALL OF THE BLOG POSTINGS</h1>

<ul class="posts">
  <% posts.each do |post| %>
    <li class="blog-post">Title: <%= post.title %> <%=link_to("Edit", posts_edit_path(post.id)) %></li>
  <% end %>
</ul>
